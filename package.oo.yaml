version: 0.1.7
scripts:
  bootstrap: |
    npm install
    poetry install --no-root
dependencies:
  alicloud-oss: 0.0.4
name: manga-zip-translator
icon: ./icon.png
displayName: "%display-name%"
description: "%description%"
ui:
  blocks:
    - url-zip-translator
    - check-zip-format
    - manga-zip-translate
    - file-zip-translator
releaseNotes: 增加上色，目录，文件名选项。输出内容增加 原始地址和云盘路径
