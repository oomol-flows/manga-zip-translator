version: 0.2.3
scripts:
  bootstrap: |
    npm install
    poetry install --no-root
dependencies:
  alicloud-oss: 0.0.4
name: manga-zip-translator
icon: ./icon.png
displayName: "%display-name%"
description: "%description%"
ui:
  blocks:
    - url-zip-translator
    - check-zip-format
    - manga-zip-translate
    - file-zip-translator
